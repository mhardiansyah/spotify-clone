import 'package:flutter/material.dart';

class ModelGenre extends StatelessWidget {
  const ModelGenre({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
